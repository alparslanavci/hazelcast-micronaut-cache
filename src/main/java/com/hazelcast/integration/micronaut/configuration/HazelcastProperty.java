package com.hazelcast.integration.micronaut.configuration;

/**
 *
 * The class for keeping the propery names for Hazelcast configuration.
 *
 */
public class HazelcastProperty {
    public static final String PREFIX = "hazelcast";
    public static final String CACHES = PREFIX + ".caches";
    public static final String INSTANCE_TYPE = PREFIX + ".caches";
}
